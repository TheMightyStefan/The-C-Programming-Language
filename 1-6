#include <stdio.h>
int main ()
{
    int c;
    c = getchar();
    while (c != EOF) {
        c = getchar();
        if ((c != EOF) != 1) {
            printf ("Impossible.");
        }
    }
    return 0;
}